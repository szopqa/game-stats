export const API_URL = 'http://localhost:3000';

/* 
 TODO: Resolve feature is not ready - I was planning to store in the API ids of resolved 
	discrepancies and assign additional flag to those. They should then be marked accordingly on the UI
*/
export const IS_RESOLVE_FEATURE_AVAILABLE = false;
